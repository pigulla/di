{
  "name": "@digitally-imported/meta",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "author": "Raphael Pigulla <pigulla@four66.com>",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "setup": "rm -rf node_modules && yarn",
    "test": "lerna run build && lerna run test && yarn audit",
    "interactive-integration-test": "ts-node --require tsconfig-paths/register test/interactive-integration.spec.ts"
  },
  "devDependencies": {
    "lerna": "^3.19.0",
    "ora": "^4.0.3"
  }
}
