{
  "name": "@digitally-imported/meta",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "author": "Raphael Pigulla <pigulla@four66.com>",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "outdated": "yarn outdated; lerna exec --no-bail 'yarn outdated'",
    "setup": "rm -rf node_modules && yarn",
    "test": "lerna run build && lerna run test && yarn audit"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}
